# 🏗️ Arquitectura del Sistema - Sacred Rebirth AI Agent

## 📊 Diagrama de Arquitectura General

```
┌─────────────────────────────────────────────────────────────────┐
│                         USUARIO                                 │
│                    (CLI / Python API)                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      main.py                                    │
│                 (Punto de Entrada)                              │
│   ┌──────────────────────────────────────────────────────┐     │
│   │  - Modo Interactivo                                   │     │
│   │  - Modo Comando                                       │     │
│   │  - Parser de Argumentos                               │     │
│   └──────────────────────────────────────────────────────┘     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    src/crew.py                                  │
│                 (MarketingCrew)                                 │
│   ┌──────────────────────────────────────────────────────┐     │
│   │  Orquesta 6 agentes especializados:                  │     │
│   │  • Estratega de Contenido                            │     │
│   │  • Creador de Contenido                              │     │
│   │  • Community Manager                                 │     │
│   │  • Especialista Email                                │     │
│   │  • Analista                                          │     │
│   │  • Customer Success                                  │     │
│   └──────────────────────────────────────────────────────┘     │
└────────────┬──────────────────────────────────┬─────────────────┘
             │                                  │
             ▼                                  ▼
┌──────────────────────────┐      ┌──────────────────────────────┐
│     src/agents.py        │      │       src/tasks.py           │
│  (Definición Agentes)    │      │   (Definición Tareas)        │
│  ┌────────────────────┐  │      │  ┌────────────────────────┐  │
│  │ - Roles            │  │      │  │ - Estrategia           │  │
│  │ - Goals            │  │      │  │ - Creación contenido   │  │
│  │ - Backstories      │  │      │  │ - Publicación          │  │
│  │ - Tools asignados  │  │      │  │ - Emails               │  │
│  │ - LLM config       │  │      │  │ - Analytics            │  │
│  └────────────────────┘  │      │  │ - Leads                │  │
└──────────┬───────────────┘      │  └────────────────────────┘  │
           │                      └──────────┬───────────────────┘
           │                                 │
           └─────────────┬───────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    src/tools.py                                 │
│              (Herramientas Personalizadas)                      │
│   ┌────────────────┬────────────────┬─────────────────┐        │
│   │ Content        │ Social Media   │ Email Campaign  │        │
│   │ Generator      │ Publish        │ Tool            │        │
│   │ Tool           │ Tool           │                 │        │
│   └────────────────┴────────────────┴─────────────────┘        │
│   ┌────────────────┬─────────────────────────────────┐         │
│   │ Content        │ Leads Manager                   │         │
│   │ Calendar Tool  │ Tool                            │         │
│   └────────────────┴─────────────────────────────────┘         │
└────────────┬────────────────────────────────┬──────────────────┘
             │                                │
             ▼                                ▼
┌──────────────────────────┐      ┌─────────────────────────────┐
│  Módulos Base            │      │  APIs Externas              │
│  ┌────────────────────┐  │      │  ┌───────────────────────┐  │
│  │ content_generator  │  │      │  │ OpenAI GPT-4          │  │
│  │ social_media       │  │      │  │ Meta Graph API        │  │
│  │ email_campaign     │  │      │  │ SendGrid API          │  │
│  │ scheduler          │  │      │  └───────────────────────┘  │
│  └────────────────────┘  │      └─────────────────────────────┘
└──────────┬───────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Almacenamiento                               │
│   ┌────────────────┬────────────────┬─────────────────┐        │
│   │ data/          │ data/          │ data/generated/ │        │
│   │ content_       │ leads.json     │                 │        │
│   │ calendar.json  │                │                 │        │
│   └────────────────┴────────────────┴─────────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de Ejecución: Campaña Completa

```
1. USUARIO INICIA CAMPAÑA
   python main.py --mode campaign --goal "Retiro Enero"
          │
          ▼
2. MAIN.PY DELEGA A CREW
   MarketingCrew.run_full_campaign()
          │
          ▼
3. CREW CREA AGENTES Y TAREAS
   ┌─────────────────────────────────────────┐
   │ Estratega → Planifica contenido semanal │
   └─────────────────┬───────────────────────┘
                     │
   ┌─────────────────▼───────────────────────┐
   │ Creador → Genera posts IG/FB            │
   └─────────────────┬───────────────────────┘
                     │
   ┌─────────────────▼───────────────────────┐
   │ Email Specialist → Crea campaña email   │
   └─────────────────┬───────────────────────┘
                     │
   ┌─────────────────▼───────────────────────┐
   │ Analista → Revisa métricas              │
   └─────────────────┬───────────────────────┘
                     │
   ┌─────────────────▼───────────────────────┐
   │ Customer Success → Nutre leads          │
   └─────────────────┬───────────────────────┘
                     │
4. CADA AGENTE USA HERRAMIENTAS
   │
   ├─→ ContentGeneratorTool → OpenAI API
   ├─→ SocialMediaPublishTool → Meta API
   ├─→ EmailCampaignTool → SendGrid API
   ├─→ ContentCalendarTool → data/content_calendar.json
   └─→ LeadsManagerTool → data/leads.json
          │
          ▼
5. RESULTADOS CONSOLIDADOS
   └─→ Retorna resultado al usuario
```

## 🧠 Flujo de Datos entre Componentes

```
┌──────────────┐
│ Configuración│
│ (.env)       │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────┐
│         config/settings.py               │
│  - API Keys                              │
│  - Business Info                         │
│  - Content Settings                      │
└──────────────┬───────────────────────────┘
               │
               ├─────────────┐
               │             │
               ▼             ▼
┌──────────────────┐  ┌─────────────────────┐
│ config/prompts.py│  │  Agentes & Tareas   │
│  - Templates     │  │  - Usan settings    │
│  - Topics        │  │  - Usan prompts     │
│  - Hashtags      │  │                     │
└────────┬─────────┘  └──────────┬──────────┘
         │                       │
         └───────────┬───────────┘
                     │
                     ▼
         ┌────────────────────────┐
         │   Herramientas (Tools) │
         │   - Ejecutan acciones  │
         │   - Llaman APIs        │
         │   - Guardan datos      │
         └────────────┬───────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │  Almacenamiento JSON   │
         │  - content_calendar    │
         │  - leads               │
         │  - generated content   │
         └────────────────────────┘
```

## 🎭 Interacción entre Agentes

```
SECUENCIAL (Process.sequential):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Agente 1 (Estratega)
    └─→ Output: Plan de contenido
         │
         ▼
    Agente 2 (Creador)
         └─→ Input: Plan de contenido
         └─→ Output: Posts generados
              │
              ▼
         Agente 3 (Community Manager)
              └─→ Input: Posts generados
              └─→ Output: Posts publicados
                   │
                   ▼
              Agente 4 (Email Specialist)
                   └─→ Output: Campaña enviada
                        │
                        ▼
                   Agente 5 (Analista)
                        └─→ Input: Datos de campaña
                        └─→ Output: Métricas y insights
                             │
                             ▼
                        Agente 6 (Customer Success)
                             └─→ Input: Leads + Insights
                             └─→ Output: Leads nutridos

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MEMORIA COMPARTIDA (Enabled):
Todos los agentes pueden acceder al contexto de agentes anteriores
```

## 🛠️ Stack Tecnológico Detallado

```
┌─────────────────────────────────────────────────────┐
│                  Aplicación                         │
│  ┌───────────────────────────────────────────────┐  │
│  │ Python 3.11+                                  │  │
│  │ ├─ main.py (CLI)                              │  │
│  │ ├─ src/crew.py (Orquestación)                 │  │
│  │ └─ src/agents.py (Definiciones)               │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│            Frameworks y Librerías                   │
│  ┌───────────────────────────────────────────────┐  │
│  │ CrewAI (0.28+)          - Agentes             │  │
│  │ LangChain (0.1+)        - LLM Chains          │  │
│  │ Pydantic                - Validación          │  │
│  │ Python-dotenv           - Config              │  │
│  │ Schedule                - Cron jobs           │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│                 APIs Externas                       │
│  ┌───────────────────────────────────────────────┐  │
│  │ OpenAI API                                    │  │
│  │ ├─ GPT-4 Turbo         - Generación           │  │
│  │ └─ Embeddings          - Memoria              │  │
│  │                                               │  │
│  │ Meta Graph API                                │  │
│  │ ├─ Instagram API       - Publicación          │  │
│  │ └─ Facebook API        - Publicación          │  │
│  │                                               │  │
│  │ SendGrid API                                  │  │
│  │ └─ Email Service       - Envío emails         │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────┐
│              Almacenamiento                         │
│  ┌───────────────────────────────────────────────┐  │
│  │ Archivos JSON                                 │  │
│  │ ├─ content_calendar.json                      │  │
│  │ ├─ leads.json                                 │  │
│  │ └─ generated/*.json                           │  │
│  │                                               │  │
│  │ Variables de Entorno (.env)                   │  │
│  │ └─ API Keys, Config                           │  │
│  └───────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## 📦 Módulos y Responsabilidades

| Módulo | Responsabilidad | Depende de |
|--------|----------------|------------|
| `main.py` | Punto de entrada, CLI, menú | `src/crew.py` |
| `src/crew.py` | Orquestación de agentes | `src/agents.py`, `src/tasks.py` |
| `src/agents.py` | Definición de agentes | `src/tools.py`, `config/settings.py` |
| `src/tasks.py` | Definición de tareas | - |
| `src/tools.py` | Herramientas personalizadas | Módulos base |
| `src/content_generator.py` | Generación con OpenAI | `config/prompts.py` |
| `src/social_media.py` | Publicación en redes | `config/settings.py` |
| `src/email_campaign.py` | Envío de emails | `config/settings.py` |
| `config/settings.py` | Configuración global | `.env` |
| `config/prompts.py` | Templates de prompts | - |

## 🔐 Seguridad y Configuración

```
.env (NO COMMITEAR)
├─ OPENAI_API_KEY          → OpenAI API
├─ META_ACCESS_TOKEN       → Meta Graph API
├─ INSTAGRAM_BUSINESS_ID   → Instagram
├─ FACEBOOK_PAGE_ID        → Facebook
└─ SENDGRID_API_KEY        → SendGrid
         │
         ▼
config/settings.py
    (Lee .env y expone constantes)
         │
         ▼
    Todo el código usa settings.py
    (Nunca accede directamente a .env)
```

## 🚀 Patrones de Diseño Implementados

1. **Factory Pattern**: `create_all_agents()` en agents.py
2. **Builder Pattern**: `MarketingCrew` construye crews dinámicamente
3. **Strategy Pattern**: Cada agente es una estrategia diferente
4. **Observer Pattern**: Memoria compartida entre agentes
5. **Template Method**: Tasks siguen estructura común
6. **Facade Pattern**: `main.py` simplifica acceso al sistema

## 📈 Escalabilidad

```
Agregar Nuevo Agente:
1. Definir en src/agents.py
2. Crear tasks en src/tasks.py
3. (Opcional) Nueva tool en src/tools.py
4. Agregar al crew en src/crew.py

Agregar Nueva Plataforma:
1. Crear módulo src/nueva_plataforma.py
2. Crear tool en src/tools.py
3. Asignar tool a agente relevante
4. Configurar API keys en .env

Agregar Nuevo Modo:
1. Crear función run_nuevo_modo() en main.py
2. Agregar opción al menú interactivo
3. Agregar a argparse choices
4. Documentar en GUIA_USO.md
```

---

Esta arquitectura modular permite:
- ✅ Fácil mantenimiento
- ✅ Escalabilidad horizontal (más agentes)
- ✅ Escalabilidad vertical (más funcionalidad por agente)
- ✅ Testing independiente de componentes
- ✅ Reutilización de código
